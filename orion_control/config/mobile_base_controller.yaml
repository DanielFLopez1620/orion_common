# mobile_base_controller.yaml
# diff_drive_controller params for the ORION mobile base.
# Wheel geometry, odometry, and velocity/acceleration limits for 100 RPM motors.

mobile_base_controller:
  ros__parameters:
    type: diff_drive_controller/DiffDriveController

    # Joint specifications
    left_wheel_names: ["coupling_11_joint"]
    right_wheel_names: ["coupling_12_joint"]

    # Wheel properties
    wheel_separation: 0.183
    wheel_radius: 0.033675
    wheel_separation_multiplier: 1.0
    left_wheel_radius_multiplier: 1.0
    right_wheel_radius_multiplier: 1.0

    # Publication of data and variance
    publish_rate: 20.0
    pose_covariance_diagonal : [0.001, 0.001, 0.0, 0.0, 0.0, 0.01]
    twist_covariance_diagonal: [0.001, 0.0, 0.0, 0.0, 0.0, 0.01]

    # Timeout for commands — aligned with ESP32 cmd_timeout_ms (1000 ms)
    cmd_vel_timeout: 1.0

    # Frames to consider
    base_frame_id: base_link
    odom_frame_id: odom
    enable_odom_tf: true

    # Control considerations
    open_loop: false

    # Command considerations
    publish_limited_velocity: true

    # Linear velocity elements
    linear.x.has_velocity_limits: true
    linear.x.has_acceleration_limits: true
    linear.x.max_velocity: 0.28  # m/s (100 RPM motors, conservative limit)
    linear.x.min_velocity: -0.28 # m/s
    linear.x.max_acceleration: 0.5 # m/s²
    linear.x.min_acceleration: -0.5 # m/s²
    linear.x.max_jerk: .NAN
    linear.x.min_jerk: .NAN

    # Angular velocity elements
    angular.z.has_velocity_limits: true
    angular.z.has_acceleration_limits: true
    angular.z.max_velocity: 0.56  # rad/s (vel_max / wheel_separation_2 ≈ 0.28 / 0.0915 ≈ 3.06, conservative: 1.5)
    angular.z.min_velocity: -0.56 # rad/s
    angular.z.max_acceleration: 1.0  # m/s²
    angular.z.min_acceleration: -1.0 # m/s²
    angular.z.max_jerk: .NAN
    angular.z.min_jerk: .NAN